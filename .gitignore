node_modules
.next
out
.env
.env.local
.env.*
.env.*.local
*.log
.DS_Store
npm-debug.log*
coverage
.prisma
/dist
/build
data/*.json
apps/web/.env
apps/web/.env.local
apps/web/.env.*

# Runtime PII store (real subscriber emails accumulate here) — never commit
apps/web/data/newsletter-subscribers.json

# Generated at build time by copy-functions.js — not source
apps/web/netlify/functions/

# Local Playwright MCP browser session artifacts (screenshots, snapshots)
.playwright-mcp/
